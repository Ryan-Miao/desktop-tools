# 🔬 插件导入导出功能测试指南

## 测试前准备

JSON 格式化插件已删除，现在你可以测试导入功能了！

---

## 📋 测试方案

### 方案一：导出 → 删除 → 导入（完整流程）

这个方案测试完整的导出和导入功能：

#### 步骤 1：导出计算稿纸插件

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **打开插件管理**
   - 点击右上角设置 ⚙️
   - 选择「插件管理」

3. **找到计算稿纸插件**
   - 查找 🧮 计算稿纸

4. **导出插件**
   - 点击 **📤** 按钮
   - 选择保存位置（如桌面）
   - 保存为 `calculator-pad.zip`

5. **确认导出成功**
   - 看到成功提示
   - 检查保存位置有 ZIP 文件

#### 步骤 2：删除计算稿纸插件（可选）

如果要测试完整的导入流程：

1. **删除插件目录**
   ```bash
   rm -rf plugins/calculator-pad
   ```

2. **重启应用**
   - 停止应用（Ctrl+C）
   - 重新运行 `npm run dev`

3. **确认插件消失**
   - 主界面不应该有计算稿纸图标
   - 插件管理中不应该显示此插件

#### 步骤 3：导入计算稿纸插件

1. **打开插件管理**

2. **点击导入按钮**
   - 点击 **📥 导入插件** 按钮

3. **选择 ZIP 文件**
   - 找到刚才导出的 `calculator-pad.zip`
   - 点击「打开」

4. **等待导入完成**
   - 自动解压和安装
   - 看到成功提示

5. **验证插件**
   - 主界面应该出现 🧮 计算稿纸图标
   - 点击可以正常使用
   - 插件管理中应该显示此插件

---

### 方案二：恢复 JSON 格式化插件测试

如果从 git 历史恢复 JSON 格式化插件：

#### 步骤 1：恢复插件文件

```bash
# 恢复最近一次删除前的 commit
git revert bf5cb90

# 或者恢复特定 commit
git checkout e97908c -- external-plugins/json-formatter/
git checkout e97908c -- src/renderer/components/json-formatter/
```

#### 步骤 2：导出 JSON 格式化插件

1. 启动应用：`npm run dev`
2. 打开插件管理
3. 找到 JSON 格式化插件
4. 点击 **📤** 导出
5. 保存为 `json-formatter.zip`

#### 步骤 3：删除并重新导入

1. 删除插件：
   ```bash
   rm -rf external-plugins/json-formatter src/renderer/components/json-formatter
   ```

2. 重新启动应用

3. 打开插件管理 → 点击 **📥 导入插件**

4. 选择 `json-formatter.zip`

5. 验证导入成功！

---

### 方案三：手动创建测试插件

创建一个简单的测试插件：

#### 步骤 1：创建测试插件目录和文件

```bash
mkdir -p plugins/test-plugin
```

创建 `plugins/test-plugin/manifest.json`：
```json
{
  "id": "com.test.plugin",
  "name": "测试插件",
  "version": "1.0.0",
  "description": "这是一个测试插件",
  "author": "Tester",
  "icon": "🧪",
  "entry": "index.ts",
  "permissions": []
}
```

创建 `plugins/test-plugin/index.ts`：
```typescript
export default {
  id: 'com.test.plugin',
  name: '测试插件'
};
```

#### 步骤 2：重启应用

```bash
npm run dev
```

#### 步骤 3：导出测试插件

1. 打开插件管理
2. 找到 🧪 测试插件
3. 点击 **📤** 导出
4. 保存为 `test-plugin.zip`

#### 步骤 4：删除并导入

1. 删除插件目录：`rm -rf plugins/test-plugin`
2. 重启应用
3. 打开插件管理 → **📥 导入插件**
4. 选择 `test-plugin.zip`
5. 验证成功！

---

## ✅ 验证清单

测试完成后，确认以下功能正常：

### 导出功能
- [ ] 导出按钮存在且可点击
- [ ] 文件选择对话框正常打开
- [ ] 可以选择保存位置
- [ ] ZIP 文件成功生成
- [ ] 成功提示显示正确路径
- [ ] ZIP 文件包含完整内容

### 导入功能
- [ ] 导入按钮存在且可点击
- [ ] 文件选择对话框正常打开
- [ ] 可以选择 ZIP 文件
- [ ] ZIP 文件成功解压
- [ ] 插件成功安装
- [ ] 成功提示显示
- [ ] 插件列表自动刷新
- [ ] 主界面显示新插件
- [ ] 新插件可以正常使用

---

## 🐛 常见问题

### 导出失败

**问题**：点击导出按钮没反应

**可能原因**：
- 插件是内置插件（只能导出本地安装的插件）
- 窗口无法打开文件对话框

**解决**：
- 确保导出的是本地安装的插件
- 查看开发者工具控制台错误信息

### 导入失败

**问题**：导入时报错

**可能原因**：
- ZIP 文件格式错误
- ZIP 缺少 manifest.json
- manifest.json 格式错误
- 插件 ID 冲突

**解决**：
- 检查 ZIP 文件完整性
- 确认 ZIP 包含 manifest.json
- 验证 JSON 格式正确
- 检查插件 ID 是否已存在

### 导入后找不到插件

**问题**：导入成功但看不到

**解决**：
1. 刷新插件列表（关闭再打开插件管理）
2. 重启应用
3. 检查过滤器设置（是否被过滤掉）

---

## 🎯 推荐测试方案

**方案三（手动创建测试插件）**最简单直接：

1. ✅ 不依赖外部文件
2. ✅ 快速创建和修改
3. ✅ 完全控制测试流程
4. ✅ 易于调试问题

**推荐执行顺序**：
1. 先用方案三测试基本功能
2. 确认导入导出功能正常
3. 再用方案一或二测试真实场景

---

## 📝 测试报告模板

测试完成后，可以填写：

```
测试方案：[方案一/二/三]
测试日期：____
测试人：____

导出测试：
- [ ] 成功导出插件
- [ ] 文件大小合理
- [ ] ZIP 包含必要文件
- 问题记录：____

导入测试：
- [ ] 成功导入插件
- [ ] 插件功能正常
- [ ] 无错误提示
- 问题记录：____

总体评价：
- 导出功能：⭐⭐⭐⭐⭐
- 导入功能：⭐⭐⭐⭐⭐
- 改进建议：____
```

---

**准备好测试了吗？开始吧！** 🚀

Made with ❤️ by Desktop Tool Team
